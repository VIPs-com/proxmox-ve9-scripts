## Proxmox VE 9 v2.0.0

- **pve-diagnostico.sh** — relatório de rede, serviços, discos (não altera o sistema)
- **pve-postinstall.sh** — repos no-subscription, NTP, upgrade (standalone por padrão)
- **pve-firewall.sh** — regras host.fw / cluster.fw (opcional)
- **docs/MANUAL.md** — manual completo do projeto

Debian 13 Trixie. Cluster opcional com 3+ nós (quórum Corosync).

```bash
bash <(curl -s https://raw.githubusercontent.com/VIPs-com/proxmox-ve9-scripts/main/utils/pve-diagnostico.sh)
bash <(curl -s https://raw.githubusercontent.com/VIPs-com/proxmox-ve9-scripts/main/scripts/pve-postinstall.sh)
```
